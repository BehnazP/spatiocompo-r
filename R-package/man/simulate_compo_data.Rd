% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_compo_data}
\alias{simulate_compo_data}
\title{Simulate Spatial Compositional Data}
\usage{
simulate_compo_data(
  n_side = 20,
  D = 3,
  kappa_true = 0.5,
  rho_true = NULL,
  alpha_true = 50,
  neumann = TRUE,
  seed = NULL
)
}
\arguments{
\item{n_side}{Grid size (creates n_side x n_side grid). Default is 20.}

\item{D}{Number of compositional components. Default is 3.}

\item{kappa_true}{True spatial range parameter. Default is 0.5.}

\item{rho_true}{True covariance matrix (d x d). Default is identity.}

\item{alpha_true}{True Dirichlet precision parameter. Default is 50.}

\item{neumann}{Use Neumann boundary conditions. Default is TRUE.}

\item{seed}{Random seed for reproducibility. Default is NULL.}
}
\value{
A list containing:
\item{y}{Observed compositional data (N x D matrix)}
\item{x_true}{True latent field (N x d matrix)}
\item{z_true}{True compositional means (N x D matrix)}
\item{G}{Base precision matrix}
\item{Q}{Full precision matrix}
\item{params}{List of true parameters (kappa, rho, alpha)}
\item{grid}{List with n_side and coordinates}
}
\description{
Generates synthetic spatial compositional data from the generative model,
useful for testing and validating the MCMC sampler.
}
\details{
The generative process is:
\enumerate{
\item Generate latent field x from \eqn{GMRF(0, (\rho \otimes Q(\kappa))^{-1})}
\item Transform to compositions: z = invalr(x)
\item Sample observations: y_i ~ Dirichlet(alpha * z_i)
}
}
\examples{
# Basic simulation
sim <- simulate_compo_data(n_side = 15, kappa_true = 0.3,
                           alpha_true = 100, seed = 42)

# Check dimensions
dim(sim$y)  # 225 x 3
dim(sim$x_true)  # 225 x 2

# Visualize one component
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)
  df <- data.frame(
    x = sim$grid$coords[, 1],
    y = sim$grid$coords[, 2],
    z1 = sim$z_true[, 1]
  )
  ggplot(df, aes(x, y, fill = z1)) +
    geom_tile() +
    scale_fill_viridis_c() +
    coord_fixed() +
    theme_minimal()
}

}
\seealso{
\code{\link[=simulate_landcover]{simulate_landcover()}} for a more realistic example,
\code{\link[=mcmc_sampling]{mcmc_sampling()}} for inference.
}
