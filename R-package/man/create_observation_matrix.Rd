% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariates.R
\name{create_observation_matrix}
\alias{create_observation_matrix}
\title{Create Observation Matrix}
\usage{
create_observation_matrix(obs_coords, grid_coords, d)
}
\arguments{
\item{obs_coords}{Matrix (n x 2) of observation coordinates (lon, lat).}

\item{grid_coords}{Matrix (N x 2) of grid coordinates, or a list with
components \code{lon} and \code{lat} vectors.}

\item{d}{Number of compositional components minus 1 (D - 1).}
}
\value{
A sparse block-diagonal matrix A of dimension (n\emph{d) x (N}d).
}
\description{
Creates the sparse observation matrix A that maps the spatial field to
observation locations.
}
\details{
Each observation is assigned to its nearest grid cell using Euclidean
distance. The matrix A has exactly one 1 per row, indicating which
grid cell each observation is associated with.
}
\examples{
# Create a 10x10 grid
grid_lon <- rep(1:10, each = 10)
grid_lat <- rep(1:10, 10)

# Some observation locations
obs_lon <- runif(20, 1, 10)
obs_lat <- runif(20, 1, 10)

A <- create_observation_matrix(
  obs_coords = cbind(obs_lon, obs_lat),
  grid_coords = cbind(grid_lon, grid_lat),
  d = 2
)
dim(A)  # (40, 200) for n*d x N*d

}
\seealso{
\code{\link[=mcmc_sampling]{mcmc_sampling()}}, \code{\link[=create_covariate_matrix]{create_covariate_matrix()}}
}
