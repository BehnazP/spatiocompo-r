% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{create_priors}
\alias{create_priors}
\title{Create Prior Specification}
\usage{
create_priors(
  G,
  d = 2,
  alpha.a = 1,
  alpha.b = 0.01,
  kappa.range = 2,
  kappa.sigma = 1,
  rho.df = NULL,
  rho.Sigma = NULL,
  logstep.MALA = 0,
  logstep.Gibbs = 0
)
}
\arguments{
\item{G}{Base precision matrix from \code{\link[=create_Q]{create_Q()}} with alpha = 1.}

\item{d}{Number of ALR components (D - 1). Default is 2 (for D = 3).}

\item{alpha.a, alpha.b}{Shape and rate parameters for Gamma prior on alpha.
Default is Gamma(1, 0.01) giving a weakly informative prior.}

\item{kappa.range, kappa.sigma}{Shape and rate parameters for Gamma prior
on kappa. Default is Gamma(2, 1).}

\item{rho.df}{Degrees of freedom for Inverse-Wishart prior on rho.
Default is d + 1 (minimally informative).}

\item{rho.Sigma}{Scale matrix for Inverse-Wishart prior. Default is
identity matrix.}

\item{logstep.MALA}{Initial log step size for MALA. Default is 0.}

\item{logstep.Gibbs}{Initial log step size for Gibbs sampler. Default is 0.}
}
\value{
A list containing all prior hyperparameters needed for
\code{\link[=mcmc_sampling]{mcmc_sampling()}}:
\itemize{
\item alpha.a, alpha.b: Gamma prior for alpha
\item kappa.range, kappa.sigma: Gamma prior for kappa
\item rho.df, rho.Sigma: Inverse-Wishart prior for rho
\item field.G: Base precision matrix
\item field.lambda: Precomputed eigenvalues for log-determinant
\item logstep.MALA, logstep.Gibbs: Initial step sizes
}
}
\description{
Helper function to create a list of prior hyperparameters with sensible
defaults for the spatial compositional model.
}
\details{
Default priors are:
\itemize{
\item alpha ~ Gamma(1, 0.01): Mean 100, weakly informative
\item kappa ~ Gamma(2, 1): Mean 2, mode at 1
\item rho ~ IW(d+1, I_d): Weakly informative, centered at identity
}

The eigenvalues of G are precomputed for efficient log-determinant
calculation during sampling.
}
\examples{
# 20x20 grid with 3 components (d=2)
G <- create_Q(c(20, 20), alpha = 1)
priors <- create_priors(G, d = 2)
names(priors)

# Custom priors for stronger spatial smoothing
priors <- create_priors(G, d = 2, kappa.range = 3, kappa.sigma = 2)

}
\seealso{
\code{\link[=mcmc_sampling]{mcmc_sampling()}}, \code{\link[=find_initial_values]{find_initial_values()}}
}
