% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derivatives.R
\name{neg_log_lik}
\alias{neg_log_lik}
\title{Negative Log-Posterior and Gradient}
\usage{
neg_log_lik(x, alpha, A, y, b, c, Q, d, w)
}
\arguments{
\item{x}{Vector of ALR-transformed latent field values, length N*d.}

\item{alpha}{Positive precision parameter.}

\item{A}{Location matrix connecting observations to grid cells.}

\item{y}{Observed compositional data (n x D).}

\item{b, c}{Hyperparameters for the Gamma prior on alpha.}

\item{Q}{GMRF precision matrix.}

\item{d}{Number of ALR components (D-1).}

\item{w}{Observation weights.}
}
\value{
A list with components:
\item{L}{Negative log-posterior value}
\item{dL}{Gradient vector of length N*d + 1 (including alpha)}
}
\description{
Computes the negative log-posterior (for minimization) and its gradient
for the spatial compositional model.
}
\details{
The posterior combines:
\itemize{
\item Dirichlet likelihood: \eqn{y_i \sim Dir(\alpha \cdot z_i)}
\item GMRF prior: \eqn{x \sim N(0, Q^{-1})}
\item Gamma prior on alpha: \eqn{\alpha \sim Gamma(b, c)}
}
}
\examples{
# Small example
G <- create_Q(c(5, 5), alpha = 1)
Qfull <- Q_rhoxQ(NULL, kappa = 0.5, G)$Q
n <- 25
x <- rnorm(n * 2)  # d = 2
y <- matrix(runif(n * 3), n, 3)
y <- y / rowSums(y)
A <- Matrix::Diagonal(n * 2)
result <- neg_log_lik(x, alpha = 10, A, y, b = 1, c = 0.01,
                      Q = Matrix::bdiag(Qfull, Qfull), d = 2, w = 1)

}
\seealso{
\code{\link[=mcmc_sampling]{mcmc_sampling()}} which uses this function.
}
